# Asks the user from the terminal for their name and prints it back to them
name = input("Whats ur name: ")
print("Yor name is: " + name)

# Asks the user from the terminal for their age and prints it back to them
# NOTE, the variable is of type STR, not INT
age = int(input("How old are you: "))

age += 1
print("You are: " + str(age) + " years old")
